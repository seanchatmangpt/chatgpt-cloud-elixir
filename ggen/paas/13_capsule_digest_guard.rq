PREFIX cce: <https://seanchatman.github.io/chatgpt-cloud-elixir/paas#>
SELECT ?run WHERE {
  ?run a cce:PaaSRun .
  FILTER NOT EXISTS { ?run cce:capsuleDigest ?digest . FILTER(STRLEN(STR(?digest)) > 0) }
}
