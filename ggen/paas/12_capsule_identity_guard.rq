PREFIX cce: <https://seanchatman.github.io/chatgpt-cloud-elixir/paas#>
SELECT ?run WHERE {
  ?run a cce:PaaSRun .
  FILTER NOT EXISTS { ?run cce:capsuleIdentity ?id . FILTER(STRLEN(STR(?id)) > 0) }
}
