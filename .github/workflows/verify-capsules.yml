name: Verify Capsules

on:
  workflow_run:
    workflows: ["Build Capsules"]
    types: [completed]
  workflow_dispatch:
    inputs:
      run_id:
        description: "Build Capsules workflow run ID"
        required: true
        type: string

permissions:
  actions: read
  contents: read

jobs:
  replay:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: beam-core
            artifact_name: chatgpt-cloud-elixir-beam-core-otp29-elixir1.20.2-linux-x86_64
          - variant: ash-core
            artifact_name: chatgpt-cloud-elixir-ash-core-otp29-elixir1.20.2-linux-x86_64
          - variant: ash-postgres
            artifact_name: chatgpt-cloud-elixir-ash-postgres-otp29-elixir1.20.2-linux-x86_64
          - variant: ash-phoenix
            artifact_name: chatgpt-cloud-elixir-ash-phoenix-otp29-elixir1.20.2-linux-x86_64
          - variant: ash-full
            artifact_name: chatgpt-cloud-elixir-ash-full-otp29-elixir1.20.2-linux-x86_64
          - variant: process-intelligence
            artifact_name: chatgpt-cloud-elixir-process-intelligence-otp27-elixir1.18.4-linux-x86_64
    steps:
      - name: Select source run
        id: run
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "id=${{ inputs.run_id }}" >> "$GITHUB_OUTPUT"
          else
            echo "id=${{ github.event.workflow_run.id }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Download manufactured artifact
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5
        with:
          name: ${{ matrix.artifact_name }}
          run-id: ${{ steps.run.outputs.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path: transported

      - name: Replay transported capsule
        shell: bash
        run: |
          set -euo pipefail
          archive="$(ls transported/chatgpt-cloud-elixir-${{ matrix.variant }}-*.tar.gz)"
          expected="$(awk '{print $1}' "$archive.sha256")"
          actual="$(sha256sum "$archive" | awk '{print $1}')"
          test "$expected" = "$actual"
          mkdir consumer
          tar -xzf "$archive" -C consumer
          CAPSULE_ARCHIVE_DIGEST="$actual" bash consumer/scripts/run-offline.sh
