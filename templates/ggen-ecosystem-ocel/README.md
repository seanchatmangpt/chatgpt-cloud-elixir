# GGEN ecosystem OCEL consumer templates

This directory is intentionally non-authoritative. `chatgpt-cloud-elixir` supplies admitted RDF facts only; `ggen-marketplace/packs/ggen-ecosystem-ocel-pack` owns the OCEL 2.0 and bounded Project #2 request templates. Generated JSON under `generated/` is a projection and must never be hand-edited.

Process discovery, conformance, fitness, precision, variants, and other process-analysis algorithms are outside this consumer and remain owned by `wasm4pm` / `wasm4pm-compat`.
