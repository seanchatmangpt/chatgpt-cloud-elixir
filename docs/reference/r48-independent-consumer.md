# R48 independent consumer qualification

`chatgpt-cloud-elixir` is a declared external consumer of the marketplace epistemic replication contract. The target base admitted by R46 is `791758248859dbf4201952294825ab324531ee0f`; current candidates must descend from that exact subject.

The repository owns only its consumer identity contract and invocation boundary. Verification logic is reused from the exact marketplace R48 candidate `4715c562c3dd2c073c0ca119edbc013647978cae`, which preserves one canonical court across consumer repositories.

The workflow is read-only, exact-head bound, non-actuating, and emits a machine-readable receipt. A producer green check does not imply consumer standing; the target-local workflow must execute on the exact consumer candidate.

`HANDWRITTEN_IRREDUCIBLE_REASON`: the local JSON identity and workflow invocation are the repository-local authority boundary. The reusable verifier belongs upstream in `ggen-marketplace` and is not duplicated here.
